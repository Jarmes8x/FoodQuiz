
<div class="container py-8">
    <div class="dashboard-content text-center">
        <h1 class="text-3xl font-bold mb-4 text-indigo-700">ยินดีต้อนรับสู่ The tale taster</h1>
        <p class="mb-6 text-gray-700">เกมตอบคำถาม 4 รสชาติในวรรณกรรมไทย<br>เริ่มต้นความสนุกได้เลย!</p>
        <% if (typeof user !== 'undefined' && user) { %>
          <a href="/dashboard" class="action-btn primary">เริ่มเล่นเกม</a>
        <% } else { %>
          <a href="/login" class="action-btn primary">เข้าสู่ระบบเพื่อเริ่มเล่นเกม</a>
        <% } %>
    </div>
</div>
